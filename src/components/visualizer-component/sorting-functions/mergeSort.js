// Importing node modules
import {v4 as uuid} from 'uuid';
import Element from '../../element-component/Element';

const mergeSort = (array, stopSort, setElementsArray, speed) => {
  return;
}

export default mergeSort;